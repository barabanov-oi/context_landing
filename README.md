# context_landing

Посадочная страница PPC-специалиста, адаптированная для запуска через Flask.

## Запуск

```bash
python -m venv .venv
source .venv/bin/activate
pip install flask
flask --app app run
```

После запуска откройте `http://127.0.0.1:5000/`.
